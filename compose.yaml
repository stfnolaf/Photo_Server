name: photo-server
x-app: &app
  build: .
  environment:
    PHOTO_S3_ENDPOINT: ${PHOTO_S3_ENDPOINT:?Set PHOTO_S3_ENDPOINT in .env}
    PHOTO_S3_BUCKET: ${PHOTO_S3_BUCKET:-photo-library}
    PHOTO_S3_ANONYMOUS: ${PHOTO_S3_ANONYMOUS:-true}
    POSTGRES_USER: ${POSTGRES_USER:-photo}
    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    POSTGRES_DB: ${POSTGRES_DB:-photo}
    PHOTO_POSTGRES_HOST: postgres
    PHOTO_POSTGRES_PORT: 5432
    PHOTO_IMPORT_ROOT: /imports
    PHOTO_DATA_DIR: /data
    PHOTO_MAX_BATCH_FILES: ${PHOTO_MAX_BATCH_FILES:-25}
    PHOTO_MAX_FILE_BYTES: ${PHOTO_MAX_FILE_BYTES:-536870912}
    AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
    AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
    AWS_SESSION_TOKEN: ${AWS_SESSION_TOKEN:-}
  volumes:
    - ${PHOTO_IMPORT_ROOT:?Set PHOTO_IMPORT_ROOT in .env}:/imports:ro
    - app-data:/data
  depends_on:
    postgres:
      condition: service_healthy
services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-photo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-photo}
    ports:
      - "127.0.0.1:55432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 2s
      timeout: 3s
      retries: 30
  api:
    <<: *app
    ports:
      - "127.0.0.1:8000:8000"
  worker:
    <<: *app
    command: ["photo-server", "worker"]
volumes:
  postgres-data:
  app-data:
